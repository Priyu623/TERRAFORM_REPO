replace Function : replace searches a given string for another given substring, and replaces each occurrence with a given replacement string.

# terraform console

# replace("1 + 2 + 3", "+", "-")

# replace("hello world", "/w.*d/", "everybody")

